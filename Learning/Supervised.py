import theano.tensor as T
import theano

import numpy as np

class Convolution:
	"""
	In: tuple of (images) 
	"""
	def __init__(self, in, out, value):
		rng = np.random.RandomState()
		if (value == None)

	"""
	Pipes output to next layer
	"""
	def pipe(self):

